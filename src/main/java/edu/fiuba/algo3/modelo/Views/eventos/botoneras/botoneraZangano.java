package edu.fiuba.algo3.modelo.Views.eventos.botoneras;

import java.util.List;

import javafx.geometry.Insets;
import javafx.scene.control.Button;
import javafx.scene.layout.HBox;

public class botoneraZangano extends HBox {
  /*botoneraZangano(){
    List<Button> buttons = crearBotones();
    HBox contenedorHorizontal = new HBox(buttons);
    contenedorHorizontal.setSpacing(10);
    contenedorHorizontal.setPadding(new Insets(25));
  }

  private List<Button> crearBotones(){
    Button construir = new Button();
    return 
  }*/
}
